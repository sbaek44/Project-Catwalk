import React from 'react';

const StyleSelector = (props) => (
  <div>
    <div>rows of 4, selecting a style updates state in the overview parent component</div>
  </div>
);

export default StyleSelector;