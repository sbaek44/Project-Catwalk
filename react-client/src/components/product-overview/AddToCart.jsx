import React from 'react';

const AddToCart = (props) => (
  <div>
    <div>dropdown 1 - size selector</div>
    <div>dropdown 2 - qty selector</div>
    <button>add to cart</button>
  </div>
);

export default AddToCart;